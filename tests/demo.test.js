const { expect } = require("chai");
describe("test suite", () => {
  it("should equal 2", () => {
    expect(1 + 1).to.equal(2);
  });
});

// describe("test suite", () => {
//   it("should equal jimmy", () => {
//     expect("").to.equal("jimmy");//   });
// });
